# Readt Starter
Quick start setup for a React project

## Setup
1. Clone the repo
`git clone ... <directory-name>`
1. Move into the directory
`cd <directory-name>`
1. Install packages
`npm i` or `yarn i`
1. Reset git
`npm run reset-git`

